module.exports = {
  // Especifica o diretório onde estão localizados os testes
  spec: 'src/**/*.spec.js',

  // Define o reporter de saída (opcional)
  reporter: 'spec',

  // Define o ambiente de execução dos testes (pode ser node ou browser)
  // Ambiente "node" é o padrão
  // environment: 'node',

  // Pode incluir outras configurações conforme necessário
};
